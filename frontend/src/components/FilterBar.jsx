// src/components/FilterBar.jsx
import React from "react";

// Filter bar component: renders inputs for message, level, resourceId, timestamps
function FilterBar({ filters, setFilters }) {
  // Handle input changes to update filters state
  // Use Tailwind for styling inputs and layout
  return (
    <div className="mb-6 flex flex-col gap-4">
      {/* Message input */}
      {/* Level select */}
      {/* ResourceId input */}
      {/* Timestamp start/end inputs */}
      {/* Optional: Clear filters button */}
    </div>
  );
}

export default FilterBar;
